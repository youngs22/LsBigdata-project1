 # 프로그램 환경 설정하기
 
 # ctrl + enter 한줄만 실행
 # shift + 드래그 블록 드래그
 
 # 오른쪽 아래에서
 # ..누르면 내가 있는 상위 디렉토리(폴더)로 이동
 # .은 현재 폴더
 
 # More에서
 # show folder in new window : 해당 위치 탐색기 뜸
 # open new terminal : 명령 프롬프트 뜨는데, Tools -> Global option에서 종류 바꿀 수 있음
 # cd(change directory) : 폴더 이동
 # 해당 폴더에 있는 파일 확인할 때  window -> dir입력 / powershell -> ls입력
 
 # tools 에서 global options 들어가서 appearance에서 글꼴 바꾸기 가능(D2 coding이 유명)
 # rsthemes 설치함 -> R studio 테마 바꾸기 가능(global option-> appearance)
 # cls: 화면 정리

# pip install pydataset -> 터미널에서 돌려야됨(파이썬 안에서 돌리면 안됨)
# !pip install pydataset -> 터미널안가고 파이썬에서 돌려도 됨
